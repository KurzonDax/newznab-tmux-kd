/**
 * Alpine.data('previewModal') - Preview/sample image modal for browse pages
 */
import Alpine from "@alpinejs/csp";

import { previewModal } from "./preview-modal-component.js";

Alpine.data("previewModal", previewModal);
