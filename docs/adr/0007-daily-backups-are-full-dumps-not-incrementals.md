# Daily backups are complete dumps of Important tables, not incrementals

The database backup feature was requested as "weekly full plus daily incremental". MariaDB has no logical incremental dump: a true incremental needs either binary logging (a server config change and restart, plus `RELOAD` / `REPLICATION SLAVE` / `BINLOG MONITOR` grants for the app's DB user, and a restore that replays a week of change logs) or `mariadb-backup` physical incrementals (a binary that is not installed, plus read access to the datadir as the `mysql` user — unreachable from the web/app user). Prod's irreplaceable data is only a few GB; the bulk of the database was Pulse telemetry (since truncated) and regenerable header-collection tables.

We decided that a **Daily backup** is a complete dump of **Important tables** only, standing on its own and restoring in one step, and that a **Full backup** is the same plus **Working tables** (collections/binaries/parts/missed_parts and their per-group variants) unless the admin turns that off. **Throwaway tables** (telemetry, logs, sessions, cache, queues, stats) are never dumped. Retention is by **Backup set** — one Full plus the Dailies until the next successful Full — and a set is kept or purged whole, so "purging a full purges its associated incrementals" holds even though nothing is incremental. The tier lists live in config, an unknown table defaults to Important, and dumps run with `--single-transaction` so the site stays online; the tmux monitor is soft-paused around the run and restored to its prior state.

Deliberate choices a future reader may be tempted to "fix":

- **Do not bolt binlog incrementals on later without revisiting the tiers.** Binlogs capture writes to every table, so they would reintroduce the Throwaway/Working churn the tiering removes, and could easily exceed a Daily dump in size. If incrementals are ever wanted, they belong behind an explicit opt-in that verifies `log_bin` and the grants at runtime and surfaces "prerequisites not met" in the admin UI.
- **The files on disk are the truth, not a database catalog.** Restoring an older dump would restore an older catalog; any table listing backups is a rebuildable cache reconciled from the Backup location on each run.
- **Off-site storage is a copy, never the Backup location itself.** The Backup location must be local, durable storage; an external or network target is reached only by the Off-site copy command, which verifies the target is a real mount with a write probe, copies temp-then-rename with checksum verification, and applies its own retention count. Pointing (or symlinking) the Backup location at a mount that can disappear was considered and rejected: a dump into an unmounted share silently lands on the local disk, and a dropped connection mid-dump leaves a torn backup.
- **Working tables default to included in the Full.** Restoring them resumes mid-collection state instead of forcing a re-collect; the admin toggle exists because on some installs they dwarf everything else.

Settled in a grilling session on 2026-08-17. See `CONTEXT.md` for **Full backup**, **Daily backup**, **Backup set**, **Important / Working / Throwaway tables**, **Backup location** and **Backup pause**.
